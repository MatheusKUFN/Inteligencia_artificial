#################################### Enunciado ####################################

Problema dos Missionários e Canibais

Há 3 missionários e 3 canibais. Há também um barco que vai da margem esquerda para a margem direita e vice-versa, sempre levando um ou duas pessoas. Todas as pessoas estão na margem esquerda e precisam ir para a margem direita. Porém, há restrições: em momento algum pode ficar mais canibais do que missionários em uma das margens. 
====================================================

Problema da travessia da ponte

Há 4 pessoas: cientista, zelador, professora e aluno. Que precisam passar pela ponte.

Cada um tem um tempo de travessia:

aluno: leva 1 minuto para cruzar a ponte
professora: leva 2 minutos
zelador: 5 minutos
cientista: 10 minutos

O problema é: qual a sequência de travessia para que o tempo total seja no máximo de 17 minutos.

Contudo, para cruzar a ponte é preciso usar uma lanterna e no máximo duas pessoas podem cruzar a ponte. Qualquer pessoa cruzando a ponte precisa segurar a lanterna.

Trabalho avaliado é a modelagem:
	1) Estados
	2) Regras de Transição
	3) Restrições
	4) Visitados
	5) Função objetivo

O que entregar?
	- colocar em um arquivo .md ou .txt
	- disponibilizar no github pessoal

#################################### Resposta ####################################

Problema dos Missionários e Canibais
3 missionários
3 canibais




Problema da travessia da ponte

# Estado:
int tempoTotal = 0
a = [E, 1]
p = [E, 2]
z = [E, 5]
c = [E, 10]
char l = 'E'
str visitados = ""

# Regras de Transição:

// Dapara fazer so com uma funcao
indo(pessoaA, pessoaB){
  char outroLado = 'D'
  if (pessoaA[0] == pessoaB[0] && pessoaA[0] == lanterna && lanterna == 'E'){
    maisLento = 0
    if (pessoaA[1] > pessoaB[1]){
      maisLento = pessoaA[1]
    }
    else{
      maisLento = pessoaB[1]
    }
    pessoaA[0] = outroLado
    pessoaB[0] = outroLado
    lanterna = outroLado
    return pessoaA, pessoaB, lanterna = outroLado, tempoTotal = maisLento, visitados += "pessoaA[0], pessoaB[0], outroLado"
  }
  elif (pessoaA[0] == lanterna && pessoaB[0] == null && lanterna == 'E'){
    pessoaA[0] = outroLadoD
    lanterna = outroLado
    return pessoaA, lanterna = outroLado, tempoTotal = pessoaA[1], visitados += "pessoaA[0], null, outroLado"
}


vindo(pessoaA, pessoaB){
  char outroLado = 'E'
  if (pessoaA[0] == pessoaB[0] && pessoaA[0] == lanterna && lanterna == 'D'){
    maisLento = 0
    if (pessoaA[1] > pessoaB[1]){
      maisLento = pessoaA[1]
    }
    else{
      maisLento = pessoaB[1]
    }
    pessoaA[0] = outroLado
    pessoaB[0] = outroLado
    lanterna = outroLado
    return pessoaA, pessoaB, lanterna = outroLado, tempoTotal = maisLento, visitados += "pessoaA[0], pessoaB[0], outroLado"
  }
  elif (pessoaA[0] == lanterna && pessoaB[0] == null && lanterna == 'D'){
    pessoaA[0] = outroLado
    lanterna = outroLado
    return pessoaA, lanterna = outroLado, tempoTotal = pessoaA[1], visitados += "pessoaA[0], null, outroLado"
  }
}


# Restrição:
No máximo duas pessoas podem cruzar a ponte (Limitado pela propria função)
pessoaA == pessoaB && pessoaA == lanterna                <-- As pessoas deve estar sempre usando a lanterna
tempoTotal <= 17                                         <-- tempo total máximo 17 minutos


# Visitados:
visitados


# Função objetivo:
a[0], p[0], z[0], c[0], l == 'D' && tempoTotal <= 17






